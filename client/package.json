{
  "name": "rtc-mesh",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --sourcemap --outfile=../coordinator/static/rtc-mesh.js",
    "dev": "esbuild src/index.ts --bundle --format=esm --sourcemap --outfile=../coordinator/static/rtc-mesh.js --watch",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
